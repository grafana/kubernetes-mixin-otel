# Instructions

- Never run `git commit` or `git push`. Leave changes staged/modified for the user to commit and push themselves.
- Never run test commands (e.g. `make test`, `go test`, `jsonnet` test targets, `bats`, etc.). The user runs tests themselves.
